import React form 'react';

const userAdd = (props) =>{
    state = {
        name:''
    }
    
    onchangeHandler = (ev) =>{
        console.log(ev)
    }
    render{
         return (<div>
           <input type="text"/>
            <button onclick={this.props.onclick}>Agregar Usuarios</button>
           </div>);
                 }
                 }
   
            